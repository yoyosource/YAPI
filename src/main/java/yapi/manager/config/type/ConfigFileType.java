package yapi.manager.config.type;

public enum ConfigFileType {
    // YAPION is for saving the Config as a YAPION file.
    YAPION,

    // YAPION is for saving the Config as a YAPION file in hierarchy structure.
    YAPIONHIERARCHY,

    // JSON is for saving the Config as a JSON file.
    JSON
}
